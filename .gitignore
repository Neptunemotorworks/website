.DS_Store
.claude/
Neptune Website/

# Internal product/brand assets — kept locally, NOT published to the public site/repo.
# The minimal holding page only needs favicon.png. Re-add deliberately if a future
# page actually uses them.
image.png
logo.png
/Logos and Names/
/assets/
**/.DS_Store
